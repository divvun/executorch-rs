//! WAVE-2 SCOPE: devtools/ is out of scope. Only the minimal
//! `bundled_program` surface referenced by extension/module/bundled_module.rs
//! (gated behind the `bundled-program` feature) is stubbed here.

pub mod bundled_program;
