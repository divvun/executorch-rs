# kernels/portable/cpu/op_asinh.cpp

> [spec:et:def:op-asinh.torch.executor.native.asinh-out-fn]
> Tensor& asinh_out(KernelRuntimeContext& ctx, const Tensor& in, Tensor& out) [generated by DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16]

> [spec:et:sem:op-asinh.torch.executor.native.asinh-out-fn]
> Implements `asinh.out(in, out)`: element-wise inverse hyperbolic sine.
> Generated by `DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16(asinh_out,
> std::asinh)`, which forwards to
> `internal::unary_ufunc_realhbbf16_to_floathbf16(std::asinh, std::asinh, ctx,
> in, out)` per
> `[spec:et:sem:unary-ufunc-realhbbf16-to-floathbf16.torch.executor.native.internal.unary-ufunc-realhbbf16-to-floathbf16-fn]`.
>
> Concretely: validates `out` is a floating type (`tensor_is_floating_type` →
> `InvalidArgument`), resizes `out` to `in.sizes()` ("Failed to resize output
> tensor." → `InvalidArgument`), checks `tensors_have_same_dim_order(in, out)`
> (→ `InvalidArgument`). Accepted input dtypes (`ET_SWITCH_REALHBBF16_TYPES`):
> {Byte, Char, Short, Int, Long, Half, Float, Double, Bool, BFloat16}; output
> dtypes (`ET_SWITCH_FLOATHBF16_TYPES`): {Half, Float, Double, BFloat16}.
> Element-wise over `in.numel()` flat elements: if CTYPE_IN is `double`, compute
> `std::asinh((double)val_in)`; otherwise `std::asinh((float)val_in)`; cast to
> CTYPE_OUT. IEEE `std::asinh` semantics: domain and range all reals; NaN yields
> NaN; `+/-inf` yields `+/-inf`. Returns `out`.

