# kernels/portable/cpu/op_acos.cpp

> [spec:et:def:op-acos.torch.executor.native.acos-out-fn]
> Tensor& acos_out(KernelRuntimeContext& ctx, const Tensor& in, Tensor& out) [generated by DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16]

> [spec:et:sem:op-acos.torch.executor.native.acos-out-fn]
> Implements `acos.out(in, out)`: element-wise inverse cosine (arccos, radians).
> Generated by `DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16(acos_out, std::acos)`,
> which forwards to `internal::unary_ufunc_realhbbf16_to_floathbf16(std::acos,
> std::acos, ctx, in, out)` per
> `[spec:et:sem:unary-ufunc-realhbbf16-to-floathbf16.torch.executor.native.internal.unary-ufunc-realhbbf16-to-floathbf16-fn]`.
>
> Concretely: validates `out` is a floating type (`tensor_is_floating_type` →
> `InvalidArgument`), resizes `out` to `in.sizes()` ("Failed to resize output
> tensor." → `InvalidArgument`), checks `tensors_have_same_dim_order(in, out)`
> (→ `InvalidArgument`). Accepted input dtypes (`ET_SWITCH_REALHBBF16_TYPES`):
> {Byte, Char, Short, Int, Long, Half, Float, Double, Bool, BFloat16}; output
> dtypes (`ET_SWITCH_FLOATHBF16_TYPES`): {Half, Float, Double, BFloat16}.
> Element-wise over `in.numel()` flat elements: if CTYPE_IN is `double`, compute
> `std::acos((double)val_in)`; otherwise compute `std::acos((float)val_in)`;
> cast the result to CTYPE_OUT. IEEE `std::acos` semantics: domain `[-1, 1]`,
> range `[0, pi]`; inputs outside `[-1, 1]` (and NaN) yield NaN. Returns `out`.

