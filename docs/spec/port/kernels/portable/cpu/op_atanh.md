# kernels/portable/cpu/op_atanh.cpp

> [spec:et:def:op-atanh.torch.executor.native.atanh-out-fn]
> Tensor& atanh_out(KernelRuntimeContext& ctx, const Tensor& in, Tensor& out) [generated by DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16]

> [spec:et:sem:op-atanh.torch.executor.native.atanh-out-fn]
> Generated by `DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16(atanh_out,
> std::atanh)`, which forwards to
> `internal::unary_ufunc_realhbbf16_to_floathbf16(std::atanh, std::atanh, ctx,
> in, out)` — see
> `[spec:et:sem:unary-ufunc-realhbbf16-to-floathbf16.torch.executor.native.internal.unary-ufunc-realhbbf16-to-floathbf16-fn]`.
> Both the float and double function pointers are `std::atanh`.
>
> Concrete behavior:
> 1. ET_KERNEL_CHECK: `out` must be a floating-point tensor
>    (tensor_is_floating_type). On failure set Error::InvalidArgument and return
>    `out` unchanged.
> 2. Resize `out` to `in.sizes()`; if resize fails set Error::InvalidArgument
>    (message "Failed to resize output tensor.") and return `out` unchanged.
> 3. ET_KERNEL_CHECK: `tensors_have_same_dim_order(in, out)`; on failure set
>    Error::InvalidArgument and return `out` unchanged.
> 4. Accept input dtype from REALHBBF16 = {Byte, Char, Short, Int, Long, Half,
>    Float, Double, Bool, BFloat16}; accept output dtype from FLOATHBF16 =
>    {Half, Float, Double, BFloat16}. Unsupported dtypes cause the type switch to
>    set Error::InvalidArgument and return `out` unchanged.
> 5. Elementwise over `in.numel()` elements in flat order: for each input value,
>    if the input C-type is `double`, compute `std::atanh` in double; otherwise
>    cast the input value to `float` and compute `std::atanh` in float. Cast the
>    result to the output C-type and store it.
> 6. std::atanh semantics: inverse hyperbolic tangent; atanh(+/-1) is +/-inf,
>    |x|>1 or NaN yields NaN, atanh(+/-0) is +/-0.
> 7. Return `out`.

