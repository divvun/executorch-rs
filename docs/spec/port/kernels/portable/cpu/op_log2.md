# kernels/portable/cpu/op_log2.cpp

> [spec:et:def:op-log2.torch.executor.native.log2-out-fn]
> Tensor& log2_out(KernelRuntimeContext& ctx, const Tensor& in, Tensor& out) [generated by DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16]

> [spec:et:sem:op-log2.torch.executor.native.log2-out-fn]
> Generated by `DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16(log2_out, std::log2)`,
> expanding to the shared unary-ufunc pattern with both float and double math
> functions set to `std::log2` (base-2 logarithm). Behavior is exactly
> `[spec:et:sem:unary-ufunc-realhbbf16-to-floathbf16.torch.executor.native.internal.unary-ufunc-realhbbf16-to-floathbf16-fn]`
> with `fn = std::log2`.
>
> Concretely: require `out` floating (ET_KERNEL_CHECK: InvalidArgument, returns
> `out`); resize `out` to `in.sizes()` (InvalidArgument, message "Failed to
> resize output tensor."); require matching dim order (InvalidArgument). Accepted
> input dtypes REALHBBF16; output dtype must be FLOATHBF16. For each element
> (row-major over `in.numel()`): double-typed input computes
> `std::log2((double)in[i])`, otherwise `std::log2((float)in[i])`, cast to the
> out ctype. NaN/inf follow `std::log2`: `log2(0)` = -inf, `log2(negative)` =
> NaN, `log2(+inf)` = +inf, `log2(NaN)` = NaN. Returns `out`.

