# kernels/portable/cpu/op_log1p.cpp

> [spec:et:def:op-log1p.torch.executor.native.log1p-out-fn]
> Tensor& log1p_out(KernelRuntimeContext& ctx, const Tensor& in, Tensor& out) [generated by DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16]

> [spec:et:sem:op-log1p.torch.executor.native.log1p-out-fn]
> Generated by `DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16(log1p_out,
> std::log1p)`, expanding to the shared unary-ufunc pattern with both float and
> double math functions set to `std::log1p` (computes `log(1 + x)` accurately for
> small `x`). Behavior is exactly
> `[spec:et:sem:unary-ufunc-realhbbf16-to-floathbf16.torch.executor.native.internal.unary-ufunc-realhbbf16-to-floathbf16-fn]`
> with `fn = std::log1p`.
>
> Concretely: require `out` floating (ET_KERNEL_CHECK: InvalidArgument, returns
> `out`); resize `out` to `in.sizes()` (InvalidArgument, message "Failed to
> resize output tensor."); require matching dim order (InvalidArgument). Accepted
> input dtypes REALHBBF16; output dtype must be FLOATHBF16. For each element
> (row-major over `in.numel()`): double-typed input computes
> `std::log1p((double)in[i])`, otherwise `std::log1p((float)in[i])`, cast to the
> out ctype. NaN/inf follow `std::log1p`: `log1p(-1)` = -inf, `log1p(x<-1)` = NaN,
> `log1p(+inf)` = +inf, `log1p(NaN)` = NaN. Returns `out`.

