# kernels/portable/cpu/op_expm1.cpp

> [spec:et:def:op-expm1.torch.executor.native.expm1-out-fn]
> Tensor& expm1_out(KernelRuntimeContext& ctx, const Tensor& in, Tensor& out) [generated by DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16]

> [spec:et:sem:op-expm1.torch.executor.native.expm1-out-fn]
> Generated by `DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16(expm1_out, std::expm1)`; delegates to
> `internal::unary_ufunc_realhbbf16_to_floathbf16(std::expm1, std::expm1, ctx, in, out)`, whose
> behavior is `[spec:et:sem:unary-ufunc-realhbbf16-to-floathbf16.torch.executor.native.internal.unary-ufunc-realhbbf16-to-floathbf16-fn]`.
> Concretely: every failure path sets the error on `ctx` and returns `out` unchanged.
> 1. ET_KERNEL_CHECK `tensor_is_floating_type(out)` (out dtype in `{Half, Float, Double, BFloat16}`), else InvalidArgument.
> 2. ET_KERNEL_CHECK `resize_tensor(out, in.sizes()) == Error::Ok`, else InvalidArgument.
> 3. ET_KERNEL_CHECK `tensors_have_same_dim_order(in, out)`, else InvalidArgument.
> 4. Elementwise, in flat index order over `in.numel()` elements: read each input value; if the
>    input dtype is `double`, compute `static_cast<CTYPE_OUT>(std::expm1(static_cast<double>(x)))`;
>    otherwise compute `static_cast<CTYPE_OUT>(std::expm1(static_cast<float>(x)))`. Input dtype ranges
>    over REALHBBF16 (`{Byte, Char, Short, Int, Long, Half, Float, Double, Bool, BFloat16}`); output
>    dtype ranges over FLOATHBF16 (`{Half, Float, Double, BFloat16}`). Computes exp(x)-1 with
>    improved accuracy near 0. NaN/inf follow `std::expm1` (expm1(+inf)=+inf, expm1(-inf)=-1,
>    expm1(NaN)=NaN).
> 5. Return `out`.

