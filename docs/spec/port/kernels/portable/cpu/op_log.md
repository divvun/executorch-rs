# kernels/portable/cpu/op_log.cpp

> [spec:et:def:op-log.torch.executor.native.log-out-fn]
> Tensor& log_out(KernelRuntimeContext& ctx, const Tensor& in, Tensor& out) [generated by DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16]

> [spec:et:sem:op-log.torch.executor.native.log-out-fn]
> Generated by `DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16(log_out, std::log)`,
> which expands to a call of the shared unary-ufunc pattern with both the float
> and double math function set to `std::log` (natural logarithm). Behavior is
> exactly `[spec:et:sem:unary-ufunc-realhbbf16-to-floathbf16.torch.executor.native.internal.unary-ufunc-realhbbf16-to-floathbf16-fn]`
> with `fn = std::log`.
>
> Concretely: require `out` to be a floating type (ET_KERNEL_CHECK: on failure
> sets Error::InvalidArgument on the context and returns `out` unchanged); resize
> `out` to `in.sizes()` (InvalidArgument with message "Failed to resize output
> tensor." on failure); require `in` and `out` share a dim order (InvalidArgument
> on failure). Accepted input dtypes are REALHBBF16 {Byte, Char, Short, Int,
> Long, Half, Float, Double, Bool, BFloat16}; the output dtype must be one of
> FLOATHBF16 {Half, Float, Double, BFloat16}. For each element index `i` in
> `[0, in.numel())` (row-major): if the input ctype is `double`, compute
> `std::log((double)in[i])` and cast to the out ctype; otherwise compute
> `std::log((float)in[i])` and cast to the out ctype. NaN/inf follow `std::log`:
> `log(0)` = -inf, `log(negative)` = NaN, `log(+inf)` = +inf, `log(NaN)` = NaN.
> Returns `out`.

