# kernels/portable/cpu/op_log10.cpp

> [spec:et:def:op-log10.torch.executor.native.log10-out-fn]
> Tensor& log10_out(KernelRuntimeContext& ctx, const Tensor& in, Tensor& out) [generated by DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16]

> [spec:et:sem:op-log10.torch.executor.native.log10-out-fn]
> Generated by `DEFINE_UNARY_UFUNC_REALHBBF16_TO_FLOATHBF16(log10_out,
> std::log10)`, expanding to the shared unary-ufunc pattern with both float and
> double math functions set to `std::log10` (base-10 logarithm). Behavior is
> exactly `[spec:et:sem:unary-ufunc-realhbbf16-to-floathbf16.torch.executor.native.internal.unary-ufunc-realhbbf16-to-floathbf16-fn]`
> with `fn = std::log10`.
>
> Concretely: require `out` floating (ET_KERNEL_CHECK: InvalidArgument, returns
> `out`); resize `out` to `in.sizes()` (InvalidArgument, message "Failed to
> resize output tensor."); require matching dim order (InvalidArgument). Accepted
> input dtypes REALHBBF16; output dtype must be FLOATHBF16 {Half, Float, Double,
> BFloat16}. For each element (row-major over `in.numel()`): double-typed input
> computes `std::log10((double)in[i])`, otherwise `std::log10((float)in[i])`,
> then cast to the out ctype. NaN/inf follow `std::log10`: `log10(0)` = -inf,
> `log10(negative)` = NaN, `log10(+inf)` = +inf, `log10(NaN)` = NaN. Returns
> `out`.

